import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#1A1A1A",
    
    },
    secondary: {
      main: "#1A1A1A",
    },
    background: {
      default: "#000",
    }, 
  },
});

export default theme;