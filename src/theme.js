import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#5B1461',
    },
    secondary: {
      main: '#1A1A1A',
    },
    background: {
      default: '#000',
    },
  },
});

export default theme;
